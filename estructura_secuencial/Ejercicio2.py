bass=float(input("Base:"))
# la variable (bass) almacena el valor de la base  y en este caso usamos la class (float) para manera los valores en decimales 
alt=float(input("Altura:"))
# la variable (alt) almacena el valor de la altura y en este caso usamos la class (float) para manera los valores en decimales 
perimetro= 2* (bass+alt)
#la variable perimetro en este caso almacena la formula para calcular el perimetro usando los valores de las variables (bass), (alt) con la calss (float dandonos un resultado en decimales)
area= bass+alt
# la variable (area) almacena la formula para hayar el area usando nuevamente los valores (bass),(alt)
print("el perimetro es ", perimetro, "y su area es ",area,)
# es la salida que obtenemos al ejecutar, obteniendo el perimetro y el area segun lo pedido en la guia 